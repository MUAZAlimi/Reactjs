// import React from 'react'

// eslint-disable-next-line react/prop-types
const ListItem = ({item}) => {
  return (
    <li>
        {JSON.stringify(item)}
    </li>
  )
}

export default ListItem
